<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - MecaniCar360</title>

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/MecaniCar360.styles.css" asp-append-version="true" />
</head>
<body>

    <header>
        @using System.Security.Claims

        <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-3">
            <div class="container-fluid">

                <a class="navbar-brand" href="/Dashboard">🚗 MEC360</a>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarMain">

                    <ul class="navbar-nav me-auto">

                        <!-- DASHBOARD -->
                        <li class="nav-item">
                            <a class="nav-link" href="/Dashboard">Inicio</a>
                        </li>

                        <!-- TURNOS -->
                        @if (!User.IsInRole("CLIENTE"))
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="/Turno">Turnos</a>
                            </li>
                        }

                        <!-- CLIENTE -->
                        @if (User.IsInRole("CLIENTE"))
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="/Turno/MisTurnos">Mis Turnos</a>
                            </li>
                        }

                        <!-- MECANICO -->
                        @if (User.IsInRole("MECANICO"))
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="/OrdenTrabajo">Ordenes</a>
                            </li>
                        }

                        <!-- STOCK -->
                        @if (User.IsInRole("STOCK"))
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="/Repuesto">Repuestos</a>
                            </li>
                        }

                        <!-- CAJA -->
                        @if (User.IsInRole("CAJA"))
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="/Pago">Pagos</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/Factura">Facturación</a>
                            </li>
                        }

                        <!-- ADMIN -->
                        @if (User.IsInRole("ADMIN"))
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="/Usuario">Usuarios</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/Reportes">Reportes</a>
                            </li>
                        }

                    </ul>

                    <!-- USER -->
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <span class="navbar-text text-white me-3">
                                👤 @User.Identity?.Name
                            </span>
                        </li>
                        <li class="nav-item">
                            <a class="btn btn-outline-light me-2" href="/Account/CambiarContraseña">Cambiar contraseña</a>
                        </li>
                        <li class="nav-item">
                            <form asp-controller="Account" asp-action="Logout" method="post">
                                @Html.AntiForgeryToken()
                                <button class="btn btn-outline-light" type="submit">Salir</button>
                            </form>
                        </li>
                    </ul>

                </div>
            </div>
        </nav>
    </header>

    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2026 - MecaniCar360
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>

    @await RenderSectionAsync("Scripts", required: false)

</body>
</html>